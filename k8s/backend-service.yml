apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: default

spec:
  selector:
    app: backend

  type: NodePort

  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30001
